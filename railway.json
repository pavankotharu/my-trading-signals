{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "streamlit run trading_signals.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --browser.gatherUsageStats false"
    }
}